# Stock LlamaIndex RAG — reuse the framework, don't rebuild it.
llama-index-core>=0.11,<0.13
llama-index-llms-openai-like>=0.2,<0.4
llama-index-retrievers-bm25>=0.3,<0.6
# BM25 = lexical retrieval => NO embedding model => no extra GPU, air-gap friendly.
transformers>=4.44         # the served model's tokenizer, so RAG chunks are 256-token-ALIGNED to the blend chunkSize
fastapi>=0.110
uvicorn[standard]>=0.29
httpx>=0.27
